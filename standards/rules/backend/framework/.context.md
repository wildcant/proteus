# `framework/`

`src/framework/` is what *runs* — the containers, the adapters, the transports. It imports `core/`,
never the reverse. See `src/framework/README.md` for what sits here; ADR-0026 records the split and
ADR-0027 the layer graph that enforces it.
