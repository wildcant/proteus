---
paths:
  - "apps/backend/src/api/**"
---

# API routes

`src/api/` is the backend's HTTP surface: the route files, the definitions that wire each handler to
its schemas and auth, and the middlewares they share. Before adding or changing an endpoint, read
`standards/rules/backend/api/__docs__/README.md` and the document it routes you to.
