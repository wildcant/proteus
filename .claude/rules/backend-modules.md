---
paths:
  - "apps/backend/src/modules/**"
---

# Modules

`src/modules/` is where a domain's tables, data access and business logic live, each module a private
container exposing exactly one service. Before adding a module, a model, a repository or a service
method, read `standards/rules/backend/modules/__docs__/README.md` and the document it routes you to.
