---
paths:
  - "apps/backend/src/workflows/**"
---

# Workflows

`src/workflows/` is where a mutation that spans two modules is orchestrated, together with the
compensation that unwinds it. Before adding or changing a workflow or one of its steps, read
`standards/rules/backend/workflows/__docs__/README.md` and the document it routes you to.
