services:
  postgres:
    image: postgres:17
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: proteus
    volumes:
      - proteus-dev-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 2s
      timeout: 5s
      retries: 10

  # A second way in to the same Postgres, for making the API behave like its database is not on
  # localhost. The proxy passes traffic through untouched until `pnpm --filter backend run dev:slow` adds a latency
  # toxic — toxiproxy's config file can declare proxies but not toxics, so that part is a curl.
  toxiproxy:
    image: ghcr.io/shopify/toxiproxy:2.12.0
    command: ['-host=0.0.0.0', '-config=/config/toxiproxy.json']
    volumes:
      - ./toxiproxy.json:/config/toxiproxy.json:ro
    ports:
      # Control API, then the slow Postgres.
      - '8474:8474'
      - '5442:5442'
    depends_on:
      postgres:
        condition: service_healthy

  # Temporal shares the postgres service above rather than bringing its own: one database server,
  # three databases (`proteus`, `temporal`, `temporal_visibility`). Its two are created and migrated
  # by `temporal-schema` below — `temporalio/server` does none of that itself, which is the whole
  # reason there are three services here where there used to be one. The deprecated
  # `temporalio/auto-setup` image did it in-process on first boot.
  #
  # The shape is the official one from
  # https://github.com/temporalio/samples-server/tree/main/compose, with the sample's own
  # `temporal-postgresql` service dropped: schema setup runs to completion, then the server boots,
  # then the `default` namespace is registered.
  #
  # `pnpm --filter backend run db:reset` only drops `proteus`, so workflow history survives a database reset —
  # `pnpm --filter backend run stack:reset` is what wipes it, along with everything else in the volume.
  temporal-schema:
    image: temporalio/admin-tools:1.31.2
    restart: on-failure:6
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DB: postgres12
      DB_PORT: '5432'
      POSTGRES_USER: postgres
      POSTGRES_PWD: postgres
      POSTGRES_SEEDS: postgres
      # `temporal-sql-tool` takes the password from SQL_PASSWORD; POSTGRES_PWD is the server's.
      SQL_PASSWORD: postgres
    volumes:
      - ./scripts/temporal/compose:/scripts:ro
    # `temporalio/admin-tools` has no /bin/bash. This has to be /bin/sh.
    entrypoint: ['/bin/sh']
    command: /scripts/setup-postgres.sh

  temporal:
    image: temporalio/server:1.31.2
    ports:
      - '7233:7233'
    environment:
      DB: postgres12
      DB_PORT: '5432'
      POSTGRES_USER: postgres
      POSTGRES_PWD: postgres
      POSTGRES_SEEDS: postgres
      DBNAME: temporal
      VISIBILITY_DBNAME: temporal_visibility
      BIND_ON_IP: 0.0.0.0
      # Resolved under /etc/temporal, where the mount below lands. The server exits at startup
      # without this file and the image does not ship one.
      DYNAMIC_CONFIG_FILE_PATH: config/dynamicconfig/development-sql.yaml
    depends_on:
      postgres:
        condition: service_healthy
      temporal-schema:
        condition: service_completed_successfully
    volumes:
      - ./temporal/dynamicconfig:/etc/temporal/config/dynamicconfig:ro
    healthcheck:
      # This image is one binary plus busybox — there is no `temporal` CLI inside it to ask for
      # cluster health the way auto-setup allowed. The port is all it can answer. The stronger
      # question is asked by `temporal-create-namespace`, which polls `operator cluster health`
      # before it does anything, so depending on *that* completing is how the rest of the stack
      # waits for SERVING.
      test: ['CMD', 'nc', '-z', 'localhost', '7233']
      interval: 5s
      timeout: 5s
      retries: 12
      # Migration is `temporal-schema`'s job now, so there is much less to do before the frontend
      # listens — the old 30s was auto-setup migrating two schemas before it started serving.
      start_period: 10s

  # The server does not register `default`; a clean boot has only `temporal-system`. Everything in
  # this repo connects to `default`, so the services that talk to Temporal wait on this one having
  # completed rather than on `temporal` being healthy.
  temporal-create-namespace:
    image: temporalio/admin-tools:1.31.2
    restart: on-failure:6
    depends_on:
      temporal:
        condition: service_healthy
    environment:
      TEMPORAL_ADDRESS: temporal:7233
      DEFAULT_NAMESPACE: default
    volumes:
      - ./scripts/temporal/compose:/scripts:ro
    entrypoint: ['/bin/sh']
    command: /scripts/create-namespace.sh

  temporal-ui:
    image: temporalio/ui:2.53.3
    ports:
      - '8088:8080'
    environment:
      TEMPORAL_ADDRESS: temporal:7233
      TEMPORAL_CORS_ORIGINS: http://localhost:8088
    depends_on:
      postgres:
        condition: service_healthy
      temporal-create-namespace:
        condition: service_completed_successfully
    healthcheck:
      test: ['CMD', 'wget', '--spider', '-q', 'http://localhost:8080/']
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s

  # What makes `pnpm --filter backend run dev` work with no extra setup. `RUNTIME` defaults to `node`, so
  # `engine-selection.ts` resolves the Temporal engine and every `createWorkflow(...).run()` becomes
  # a task on the `proteus` queue. Nothing polling that queue is not an error — `workflow.execute`
  # has no execution timeout, so the HTTP request simply never returns. This is what polls it.
  #
  # Iterating on workflow or Worker code means running it on the host instead: `worker:dev` is the
  # same entrypoint under `tsx --watch`, so an edit restarts it in ~5s rather than by hand. Stop this
  # service first — both poll `proteus`, and whichever is free claims the task. The VS Code `dev`
  # task does that stop for you.
  #
  # Watch mode deliberately does not live in here. The bind mount below carries host writes into the
  # container, but not the filesystem events that a watcher needs, so `--watch` in this service would
  # look like it worked and then never fire.
  worker:
    build:
      context: ../..
      dockerfile: apps/backend/Dockerfile.worker
    depends_on:
      postgres:
        condition: service_healthy
      temporal-create-namespace:
        condition: service_completed_successfully
    environment:
      # dotenvx leaves an already-set variable alone, so these win over `.env.local` without
      # `--overload`. Both addresses are the same services the host reaches on localhost; inside the
      # network they are service names.
      POOLER_DATABASE_URL: postgres://postgres:postgres@postgres:5432/proteus
      DIRECT_DATABASE_URL: postgres://postgres:postgres@postgres:5432/proteus
      TEMPORAL_ADDRESS: temporal:7233
    volumes:
      # The Worker resolves the same secrets the API does, and reads them the same way — `pnpm
      # setup` (specifically `pull-keys`) has to have produced `.env.keys`, exactly as it must for
      # `pnpm --filter backend run dev`.
      - ../../.env.local:/app/.env.local:ro
      - ../../.env.keys:/app/.env.keys:ro
      # Source only. `node_modules` stays the image's, because `@temporalio/core-bridge` is a native
      # addon and the host's build is for the host's platform.
      - ./src:/app/apps/backend/src:ro
      - ../../packages:/app/packages:ro
      # `upload-files` writes through the localfs provider in dev, and the API serves what it wrote.
      # Without this the two disagree about where the file went.
      - ./static:/app/apps/backend/static
    healthcheck:
      # Asks Temporal whether the queue has a poller, rather than whether a process exists — a
      # Worker that booted and failed to connect is exactly the case the second one misses.
      test: ['CMD', 'pnpm', '--silent', '--filter', 'backend', 'run', 'temporal:worker-ready']
      interval: 5s
      timeout: 20s
      retries: 12
      # The workflow bundle is a webpack pass over the sandbox entrypoint, so the first probe is
      # late rather than slow — the same reason `temporal` above needs one.
      start_period: 60s

  # The events Worker. Same image as `worker` above, a different entrypoint and a different queue:
  # this one polls `proteus-events` and runs subscribers, that one polls `proteus` and runs workflow
  # steps. Two services rather than two queues on one, so a burst of event deliveries cannot take the
  # slots a shopper's checkout step is waiting for.
  #
  # It joins the stack for the same reason `worker` did: `RUNTIME` defaults to `node`, so
  # `adapter-selection.ts` resolves the Temporal event bus and every `bus.emit(...)` becomes a
  # standalone activity on `proteus-events`. A queue nobody polls does not fail — the event simply
  # waits, durably, until something does.
  #
  # No healthcheck. `temporal:worker-ready` asks about the `proteus` queue specifically, and nothing
  # else in the stack waits on this service.
  events-worker:
    build:
      context: ../..
      dockerfile: apps/backend/Dockerfile.worker
    command: ['pnpm', '--filter', 'backend', 'run', 'worker:events']
    depends_on:
      postgres:
        condition: service_healthy
      temporal-create-namespace:
        condition: service_completed_successfully
    environment:
      POOLER_DATABASE_URL: postgres://postgres:postgres@postgres:5432/proteus
      DIRECT_DATABASE_URL: postgres://postgres:postgres@postgres:5432/proteus
      TEMPORAL_ADDRESS: temporal:7233
    volumes:
      - ../../.env.local:/app/.env.local:ro
      - ../../.env.keys:/app/.env.keys:ro
      - ./src:/app/apps/backend/src:ro
      - ../../packages:/app/packages:ro
      - ./static:/app/apps/backend/static

  # The cron Worker. Same image again, a third entrypoint and a third queue: this one polls
  # `proteus-cron` and runs the jobs in `src/jobs/`. Three services rather than three queues on one,
  # so a nightly job that takes an hour cannot hold a slot a shopper's checkout step is waiting for,
  # and a checkout burst cannot delay a tick.
  #
  # It joins the stack because the API only *schedules*. `scheduler.start(jobs)` writes one Temporal
  # Schedule per job at boot and starts no Worker, so a stack without this service creates every
  # schedule and answers none of them: the run is started, waits durably for a poller, and then
  # fails on its own timeout — and under overlap `SKIP` that failed run also suppresses the ticks
  # behind it. This is what polls it.
  #
  # Unlike `events-worker` it has a healthcheck, and the difference is forced rather than a
  # preference: a Schedule's action can only start a *workflow*, so this Worker registers one and
  # pays the same webpack pass over the sandbox entrypoint that `worker` does. Without a probe
  # `--wait` would return while that bundle is still building, which is exactly the window in which
  # the cron queue has no poller on it.
  cron-worker:
    build:
      context: ../..
      dockerfile: apps/backend/Dockerfile.worker
    command: ['pnpm', '--filter', 'backend', 'run', 'worker:cron']
    depends_on:
      postgres:
        condition: service_healthy
      temporal-create-namespace:
        condition: service_completed_successfully
    environment:
      POOLER_DATABASE_URL: postgres://postgres:postgres@postgres:5432/proteus
      DIRECT_DATABASE_URL: postgres://postgres:postgres@postgres:5432/proteus
      TEMPORAL_ADDRESS: temporal:7233
    volumes:
      - ../../.env.local:/app/.env.local:ro
      - ../../.env.keys:/app/.env.keys:ro
      - ./src:/app/apps/backend/src:ro
      - ../../packages:/app/packages:ro
      - ./static:/app/apps/backend/static
    healthcheck:
      # `cron` is the Worker to ask about, not the queue to ask for: the argument resolves to
      # `CRON_TASK_QUEUE` inside the script, so the queue string stays in
      # `src/framework/scheduler/temporal/config.ts` and a compose file cannot name a queue nothing
      # polls. Bare — as the `worker` service calls it — it still means the workflow queue.
      test: ['CMD', 'pnpm', '--silent', '--filter', 'backend', 'run', 'temporal:worker-ready', 'cron']
      interval: 5s
      timeout: 20s
      retries: 12
      # The same reason `worker` needs one: the first probe is late rather than slow, because the
      # workflow bundle has to be built before anything polls.
      start_period: 60s

volumes:
  proteus-dev-data:
