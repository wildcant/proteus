# Admin app

Read the relevant file before working in a subdirectory:

- **Components** (`src/components/`, `features/*/components/`): `standards/rules/frontend/components/.context.md`
- **Feature API hooks** (`features/*/api/`): `standards/rules/frontend/features/api/.context.md`
- **Feature hooks** (`features/*/hooks/`): `standards/rules/frontend/features/hooks/.context.md`
