{
  "name": "ports-and-adapters-prototype",
  "private": true,
  "workspaces": [
    "apps/backend",
    "apps/store",
    "apps/admin",
    "packages/*"
  ],
  "scripts": {
    "setup": "npm install && npm run pull-keys && npm run gen:workerd-env",
    "gen:workerd-env": "dotenvx get -f .env.local --format eval 2>/dev/null > apps/backend/.env.workerd",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "check": "biome check .",
    "openapi:generate": "npm run --workspace=backend openapi:dump:offline && npm run --workspace=admin openapi:client && npm run --workspace=store openapi:client",
    "typecheck": "npm run --workspace=backend typecheck && npm run --workspace=store typecheck && npm run --workspace=admin typecheck",
    "verify": "./scripts/verify.sh",
    "check:workflow-purity": "tsx scripts/checks/replay-purity.ts",
    "sync-secrets:admin": "./scripts/sync-secrets.sh proteus-admin",
    "sync-secrets:backend": "./scripts/sync-secrets.sh proteus-backend",
    "sync-secrets:store": "./scripts/sync-secrets.sh proteus",
    "pull-keys": "npx wrangler r2 object get dotenv-keys/.env.keys --file=.env.keys --remote"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4",
    "tsx": "^4.19.0",
    "typescript": "^6.0.2"
  }
}
