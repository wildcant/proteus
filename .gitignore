node_modules
.env.keys
.env.keys.production
*.db

# Prisma generated client (regenerate with: npx prisma generate)
backend/generated/

tmp

# Local filesystem file provider upload directory
apps/backend/static
.wrangler
.env.workerd

.playwright-mcp

# Vitest Browser Mode writes these when a component test fails — a local debugging aid, and
# regenerated on the next failing run.
.vitest-attachments/
**/__screenshots__/

# Personal, per-project agent instructions. `AGENTS.local.md` is the source of truth and
# `CLAUDE.local.md` is a symlink to it, kept only until Codex reads the AGENTS-named one
# (openai/codex#26957). Both are untracked on purpose: project conventions go in AGENTS.md,
# where a reviewer can see them.
AGENTS.local.md
CLAUDE.local.md
