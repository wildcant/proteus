node_modules
.env.keys
.env.keys.production
*.db

# Prisma generated client (regenerate with: pnpm dlx prisma generate)
backend/generated/

tmp

# Local filesystem file provider upload directory
apps/backend/static
.wrangler
.env.workerd

.playwright-mcp

# Vitest Browser Mode writes these when a component test fails — a local debugging aid, and
# regenerated on the next failing run.
.vitest-attachments/
**/__screenshots__/

# Personal agent instructions. CLAUDE.local.md links to the Codex copy so both agents
# share the same untracked working agreements.
.codex/AGENTS.md
CLAUDE.local.md

__pycache__