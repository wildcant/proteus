node_modules
.env.keys
.env.keys.production
*.db

# Prisma generated client (regenerate with: pnpm dlx prisma generate)
backend/generated/

tmp

# Local filesystem file provider upload directory
apps/backend/static
.wrangler
.env.workerd

.playwright-mcp

# Vitest Browser Mode writes these when a component test fails — a local debugging aid, and
# regenerated on the next failing run.
.vitest-attachments/
**/__screenshots__/

# Personal agent overrides. AGENTS.override.md is the source of truth (read by Codex);
# CLAUDE.local.md is a symlink to it (read by Claude Code). Both are untracked.
AGENTS.override.md
CLAUDE.local.md
